{
  "name": "neuroplay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "dev:backend": "npm --prefix backend start",
    "build": "npm --prefix dashboard run build",
    "check": "npm --prefix backend run check && npm --prefix dashboard run build"
  },
  "engines": {
    "node": ">=20"
  }
}
